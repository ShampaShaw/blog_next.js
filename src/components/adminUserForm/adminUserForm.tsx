import styles from './adminUserForm.module.css';

const AdminUserForm = () => {
    <div className={styles.container}>
        <h1>Admin const first = useRef(second) Form</h1>
    </div>
}

export default AdminUserForm;